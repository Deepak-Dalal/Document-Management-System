const UNEXPECTED_FILE_TYPE = {
  code: "UNEXPECTED_FILE_TYPE",
  message: "Only following file types are allowed: jpeg,jpg,png,gif,csv,pdf",
};

const ONE_MB_IN_BYTES = 1048576;

module.exports = { UNEXPECTED_FILE_TYPE, ONE_MB_IN_BYTES };
